sm_devzones_mode "1" // 0 = Use checks every X seconds for check if a player join or leave a zone, 1 = hook zone entities with OnStartTouch and OnEndTouch (less CPU consume)
sm_devzones_checker "0.5" // checks and beambox refreshs per second, low value = more precise but more CPU consume, More hight = less precise but less CPU consume
sm_devzones_model "models/props/cs_assault/oxy_torch.mdl" // Use a model for zone entity, if this model not exist in your game, change to other model (IMPORTANT: change this value only on map start
sv_gravity 800